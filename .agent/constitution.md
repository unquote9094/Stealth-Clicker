# 🟢 AGENT CONSTITUTION (핵심 규칙)

> **SYSTEM**: 이 규칙은 모든 AI 에이전트가 따라야 하는 **절대 규칙**입니다.

---

## 1. 언어

- **모든 답변/주석/커밋**: 한국어 (변수명, 기술용어 제외)

---

## 2. GitHub Issue = 유일한 진실 (Single Source of Truth)

| 규칙 | 설명 |
|:---|:---|
| **이슈 먼저** | 코드 수정 전 반드시 GitHub 이슈 생성/확인 |
| **이슈 번호 필수** | 커밋 메시지에 `#N` 포함 필수 |
| **차단** | 이슈 번호 없이 커밋 시 **거부됨** |

---

## 3. 이슈 작성 규칙 (절대 규칙)

> [!CAUTION]
> **오랜만에 봐도, 다른 사람이 봐도 이해 가능해야 함!**

### 필수 포함 항목

1. **한줄 요약** - 비개발자도 이해할 수 있게
2. **관련 파일 링크** - 나중에 찾기 쉽게
3. **진행 기록** - 언제 무엇을 했는지
4. **완료 조건 체크리스트** - 끝났는지 명확히

### 금지 사항

- ❌ "버그 수정함" (어떤 버그?)
- ❌ "기능 추가" (어떤 기능?)
- ❌ 파일 링크 없는 설명

---

## 4. 세션 핸드오버

| 트리거 | 액션 |
|:---|:---|
| 스텝 300 도달 | `HANDOVER.md` 작성 시작 |
| 스텝 400 도달 | 작업 중단 후 핸드오버 완료 |
| **사용자 `/handover`** | **즉시 저장** (먹통 대비) |

### 필수 포함 정보 (중요도 순)

1. **현재 작업** - 지금 뭘 하고 있는지 (한 줄)
2. **수정 중인 파일** - 정확한 경로
3. **이슈 번호** - #N
4. **다음에 할 것** - 체크리스트
5. 최근 커밋/수정 파일 목록 (git status)

### ⚡ 먹통 대비 빠른 저장

시간 없으면 최소한 이것만:

```
현재 작업: [파일] - [하던 것] (#N)
다음: [할 일]
```

---

## 5. 터미널 먹통 방지 (Anti-Deadlock)

| 명령 | 규칙 |
|:---|:---|
| `gh issue *` | `command_status` 호출 **스킵** |
| `gh pr *` | `command_status` 호출 **스킵** |
| `git push` | **5초 이내** 대기 후 진행 |

---

## 6. 모듈화 태그

`src/*.js` 수정 시 커밋 메시지에 태그 필수:

| 태그 | 의미 |
|:---|:---|
| `[MOD]` | 모듈화 완료 |
| `[RAW]` | 의도적으로 모듈화 안 함 |
| `[N/A]` | 해당 없음 |

---

> **SYSTEM MESSAGE**: 규칙 위반 시 `process.exit(1)`.
