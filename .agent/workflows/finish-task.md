---
description: 작업 완료 시 커밋 및 GitHub 동기화 (필수)
---

# 🎯 FINISH-TASK (필수)

> **STATUS**: 필수 - 이슈 번호 없으면 **차단됨**

## 실행 방법

```bash
npm run finish -- "type: 메시지 (#N)"
```

### 예시

```bash
npm run finish -- "fix: 제목 추출 버그 수정 (#5)"
npm run finish -- "feat: 멀티 다운로드 구현 (Closes #10) [MOD]"
```

## 자동으로 되는 것

| 단계 | 내용 |
|---|---|
| 1 | 이슈 번호 확인 (없으면 차단!) |
| 2 | `npm test` 실행 |
| 3 | `git add . && commit && push` |

## 커밋 메시지 규칙

- `#N` - 이슈 참조 (이슈 열린 상태 유지)
- `Closes #N` - 이슈 자동 Close
- `[MOD/RAW/N/A]` - 모듈화 태그 (src 수정 시)

---

> [!CAUTION]
> **gh 명령 실행 후 `command_status` 확인 스킵!** (먹통 방지)
